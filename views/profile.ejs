<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex justify-end">
            <a class="bg-red-500 rounded-md px-5 py-3 text-sm mb-5 inline-block" href="/logout">Logout</a>
        </div>
        <h3 class="text-3xl mb-5"> <span class="font-light">Hello,</span> <%= user.name %></h3>
        <h5 class=" mb-5">you can create a post</h5>
        <form action="/post" method="post">
            <textarea placeholder="what's on your mind" class="w-2/3  block p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content"></textarea>
            <input class="w-40 px-3 py-2 bg-blue-500 rounded-md block mt-2 text-sm " type="submit" value="create  new post">
        </form>

        <!-- post section -->
         <div class="posts mt-20">
            <h3 class="text-zinc-400 text-lg">Your Posts.</h3>
            <div class="postcontainer mt-5">

                <% user.posts.reverse().forEach(function(post) { %>
                
                    <div class="post mb-4 w-1/3 p-5 rounded-md border-2 border-zinc-700 bg-zinc-800">
                    <h4 class="text-blue-500 mb-2"> <%= user.username %></h4>
                    <p class="text-sm tracking-tight"> <%= post.content %></p>
                    <div class="btns flex gap-4 mt-5">
                        <a class="text-blue-500" href="">Like</a>
                        <a class="text-zinc-500" href="">edit</a>
                    </div>
                </div> 
                <% }) %>
            </div>
         </div>
    </div>
</body>
</html>